#setoutputsize(640,480);

setbgcolor(white());
setautostrokecolor(blue());
setautostroke();

setlinewidth(3);
for(a=[1:150:3])
{
//test
	moveto(10, 10);
	color(black())
	{
	curve(10+10*a, 10, 600-10*a, 400, 600, 400);
	stroke();
	}
}
rectangle(10, 10, 600-10, 400-10);
